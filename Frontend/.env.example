# Set to your backend base URL (no trailing /api)
VITE_API_BASE_URL=http://localhost:5000

# The frontend expects backend report endpoints:
# GET  /api/reports/doctors?period=daily|weekly|monthly
# GET  /api/reports/specialties?period=...
# GET  /api/reports/companies?period=...
#
# Ensure CORS is enabled for http://localhost:3000 during development.